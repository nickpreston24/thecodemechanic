@model HydroFooter

<footer class="footer sm:footer-horizontal footer-center bg-base-200 text-base-content rounded p-10">
    <nav class="grid grid-flow-col gap-4">
        @Model.Slot("links")
    </nav>
    <nav>
        <div class="grid grid-flow-col gap-4">
            @Model.Slot("logos")
        </div>
    </nav>
    <aside>
        @Model.Slot("copyright")
    </aside>
</footer>